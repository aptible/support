/ Template: category_page.haml
= wrap_layout :layout do
  %header.primary-header.full-header
    .primary-header-bg
      = partial 'layouts/navbar'
      - if current_page.data.sub_title
        .container.header-title-container
          %h1.primary-title= current_page.data.title
          %h2.primary-sub-title= current_page.data.sub_title
  = yield
  - #.main
  - #  %ul
  - #  - current_page.children.each do |framework|
  - #    %li= link_to(framework.data.title, framework)
  - #  = yield
